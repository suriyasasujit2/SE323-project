@model IEnumerable<WatDuangDee.Domain.Entities.Gallery>

@{
    ViewBag.Title = "Index";
}

<h2>Index Album Management (หน้าจัดการอัลบั้ม)</h2>

<p>
    @Html.ActionLink("Create New (เพิ่มอัลบั้ม)", "Create")
</p>
<table class="table">
    <tr>
        <th>
           @Html.Label("Filckr Emded Code")
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Photo_Path)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.GalleryId }) |
            @Html.ActionLink("Details", "Details", new { id=item.GalleryId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.GalleryId })
        </td>
    </tr>
}

</table>
